<%= form_with(model: saved_weather, local: true) do |form| %>
  <% if saved_weather.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(saved_weather.errors.count, "error") %> prohibited this saved_weather from being saved:</h2>

      <ul>
      <% saved_weather.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :search_city %>
    <%= form.text_field :search_city %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
